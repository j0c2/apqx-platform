# Self-signed ClusterIssuer for cert-manager
# Provides self-signed TLS certificates for development

apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: selfsigned-cluster-issuer
spec:
  selfSigned: {}