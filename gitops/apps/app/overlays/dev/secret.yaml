# Placeholder Secret for sample-app (DEV ONLY)
# 
# ⚠️  WARNING: This contains plaintext secrets and is only for development
# 
# For production, use one of:
# - SealedSecrets: https://sealed-secrets.netlify.app/
# - SOPS: https://toolkit.fluxcd.io/guides/mozilla-sops/
# - External Secrets Operator: https://external-secrets.io/
# - Kubernetes External Secrets
#
# This file should be replaced in production overlays

apiVersion: v1
kind: Secret
metadata:
  name: sample-app-secrets
  namespace: sample-app
  labels:
    app.kubernetes.io/name: sample-app
    app.kubernetes.io/component: secret
    app.kubernetes.io/part-of: apqx-platform
    environment: dev
type: Opaque
stringData:
  # Database connection (placeholder values)
  DB_HOST: "localhost"
  DB_PORT: "5432"
  DB_NAME: "sample_app_dev"
  DB_USER: "app_user"
  DB_PASSWORD: "dev_password_replace_in_prod"
  
  # API keys (placeholder values)
  API_KEY: "dev-api-key-12345"
  JWT_SECRET: "dev-jwt-secret-replace-with-secure-value"
  
  # Application config
  APP_ENV: "development"
  LOG_LEVEL: "debug"