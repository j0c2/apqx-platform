# Dev overlay for sample application
# Customizations specific to the development environment

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: sample-app

resources:
- ../../base
- service-stable.yaml
- service-canary.yaml
- rollout.yaml
- ingress.yaml
- secret.yaml
- clusterissuer-selfsigned.yaml
- certificate.yaml
- ts-egress-externalname.yaml
- ingress-tailscale.yaml

# Image configuration - digest will be updated by CI pipeline
images:
- digest: sha256:c6b51837bb059ac2f06d7fa8add86e0943f64b44f12e6ec240903b0334dbaab9
  name: ghcr.io/j0c2/apqx-platform/sample-app

labels:
- pairs:
    environment: dev
